{
  "dependencies": {
    "prompt-sync": "^4.2.0",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "exec": "npm run build && npm run start",
    "start": "live-server",
    "build": "tsc"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
